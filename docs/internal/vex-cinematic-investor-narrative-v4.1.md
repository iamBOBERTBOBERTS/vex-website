# VEX — Advanced GLSL & Cinematic Apex GLSL Ultra (internal, v4.1)

**Confidential — internal positioning only.**

## Elevator pitch

VEX is the only dealer platform whose **public website** reads like **native hypercar configurator software** — not a brochure with a payment form. The moat is **GPU code**: thin-film paint, procedural pearl flake, anisotropic chrome, and multi-layer clear-coat, all **tenant-tunable** from JSON and live sliders.

## KPI mapping (hypothesis)

| Outcome | Target | Mechanism |
|---------|--------|-----------|
| Hero dwell | +70% | Mouse-linked iridescence + sparkle + scroll god-rays |
| Configurator sessions | 5× depth | `clearCoatRefraction`, `anisotropyStrength`, glass UI |
| Checkout intent | 3× | Emotional peak before Stripe (Apex CTA sync) |
| White-label sign-up | 7× vs legacy DMS | Branded uniforms + HDRI preset without a rebuild |

## Monetization: “Cinematic Apex GLSL Ultra”

- Custom flake / iridescence presets, dealer HDRIs, environment bundles.
- Same CRM contract: `TenantCinematic3d` fields map to runtime uniforms.

## Versus legacy stacks

Dealertrack-class sites ship **flat CSS**. VEX ships **signed distance to showroom**: every frame runs **Belcour-style thin-film phases** and **3D fbm sparkle** on the actual vehicle mesh — proof in `/` and `/configure`.

## One-line close

**Luxury is measured in shader instructions per pixel — and VEX owns the stack.**
