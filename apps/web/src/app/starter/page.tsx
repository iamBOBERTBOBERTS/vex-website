import WebsiteStarter from "@/components/WebsiteStarter";

export const metadata = {
  title: "Website Starter | VEX",
  description: "A reusable starter landing page preview built inside the VEX web app.",
};

export default function StarterPage() {
  return <WebsiteStarter />;
}
