export const typography = {
  display: "var(--font-display)",
  body: "var(--font-body), var(--font-inter), system-ui, sans-serif",
  nav: "var(--font-poppins), var(--font-inter), system-ui, sans-serif",
  heroTracking: "-0.05em",
  sectionTracking: "-0.05em",
  bodyLineHeight: "1.8",
  labelTracking: "0.28em",
} as const;
